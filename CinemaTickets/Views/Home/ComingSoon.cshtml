@{
    var movies = ViewBag.comingSoonMovies as List<CinemaTickets.Models.Movie>;
}

@if (movies.Count > 0)
{
    <div class="violet text-uppercase bold coming-soon-title">... Скоро на екранах</div>
    <div class="movies-slider-prev slider-prev grey">
        <span class="glyphicon glyphicon-menu-left"></span>
    </div>
    <div class="movies">
        @foreach (var movie in movies)
        {
            <div class="movie-item" data-youtube-url="@movie.YoutubeUrl">
                <div class="movie">
                    <div class="movie-image" style="background-image: url('/images/@movie.Image')"></div>
                    <div class="movie-trailer pointer">
                        <div class="movie-trailer-button">
                            <span>
                                <span class="glyphicon glyphicon-play"></span>
                            </span>
                        </div>
                        <div class="movie-trailer-label">
                            Трейлер
                        </div>
                    </div>
                    <div class="movie-name">
                        @movie.Name
                    </div>
                </div>
            </div>
        }
    </div>
    <div class="movies-slider-next slider-next grey">
        <span class="glyphicon glyphicon-menu-right"></span>
    </div>
    <div class="movie-trailer-popup hidden">
        <span class="glyphicon glyphicon-remove-circle movie-trailer-popup-close violet pointer"></span>
        <iframe></iframe>
    </div>
}